# Mahala CHIRPS pipeline dependencies.
# Everything else (downloading, gzip, CSV, .env parsing) uses the Python standard library.
#
# Pinned to the versions the pipeline was validated against, so a fresh install
# reproduces the committed outputs. rasterio ships GDAL/PROJ in its wheels, so a
# plain `pip install` works on macOS/Windows/Linux without a system GDAL.
numpy==2.4.6
rasterio==1.5.0

# tools/google_sheets_lookup.py builds the Excel workbook.
openpyxl==3.1.5

# Optional: only for the NDVI sub-pipeline (ndvi_run_pipeline.py), which
# pulls Sentinel-2 NDVI from Google Earth Engine and writes a Google Sheets tab.
# earthengine-api pulls in google-auth + google-api-python-client transitively;
# pin them so the raw Sheets v4 client is reproducible. gspread is NOT needed.
# google-cloud-storage is only for the optional run archive (NDVI_GCS_ARCHIVE);
# it reuses the Earth Engine service-account credentials.
# Uncomment to install (the CHIRPS pipeline does not need these).
# earthengine-api==1.4.3
# google-api-python-client==2.149.0
# google-auth==2.36.0
# google-cloud-storage==2.19.0
